let somar = (a, b) => a + b;
let subtrair = (a, b) => a - b;
let multiplicar = (a, b) => a * b;
let dividir = (a, b) => a / b;

module.exports ={
    somar,
    subtrair,
    multiplicar,
    dividir
}